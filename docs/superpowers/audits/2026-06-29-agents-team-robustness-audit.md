# Agents-Team 鲁棒性审计与修复记录

日期：2026-06-29

## 审计结论

Agents-Team 的方向是正确的：通用机制放在 Plugin，项目级规则、Issue/PR 模板、验证脚本和 CI 承担落地。但首版仍有几个会影响即插即用的薄弱点。

## 发现的问题

### P1：分发包不能完全自证

解包 `dist/agents-team-0.1.0.zip` 后运行插件测试会失败。原因是包内包含插件测试，但没有包含仓库根目录的 `tools/build_distribution.py`。这会让使用者无法从分发包复现验证过程。

### P1：初始化覆盖策略过宽

旧逻辑只要目标文件包含 `Generated by agents-team` 标记，就允许覆盖。项目团队一旦手工调整生成模板，再次初始化或升级时就可能丢失本地修改。

### P1：PR 证据契约不够硬

PR 模板要求填写证据，但没有自包含检查器。Codex 仍可能保留 `Closes #`、`待验收` 或空证据，然后把“模板存在”误判为“流程合格”。

### P1：PR 与 Issue 没有闭环验证

只检查 PR 正文还不够。真正的协作契约在 Goal Issue 中，如果 PR 不能反查 Issue，就无法确认 Goal、必须完成、验收门禁和任务边界是否存在。

## 本轮修复

- 分发构建脚本将 `tools/` 纳入包内，让压缩包可以自证。
- 分发验证脚本在解包后重新运行插件测试，再执行一次临时项目初始化和自校验。
- 初始化覆盖策略改为基于 `managedFiles` 哈希判断；本地漂移和非受控文件会阻止覆盖。
- 新增 `validate_pr_contract.py`，用于检查 PR 正文是否包含关联 Issue、Goal 对照、必须完成证据、验收门禁结果、任务边界检查、QA 结论、剩余风险和回滚方式。
- PR 模板升级为更明确的证据结构。
- `validate_pr_contract.py` 支持读取 GitHub pull_request event，并通过 GitHub API 反查 `Closes/Fixes/Resolves #编号` 指向的 Issue。
- 被链接的 Issue 必须包含 Goal、必须完成、验收门禁、任务边界、风险等级、依赖与阻塞条件。
- L3 Issue 必须额外包含用户确认、方案和回滚。
- generated `collaboration-gate.yml` 已接入 PR 与 Issue 契约检查。

## 验收口径

```bash
cd plugins/agents-team
python3 -m unittest discover -s tests -v

cd ../..
python3 tools/build_distribution.py --output dist/agents-team-0.1.0.zip
python3 tools/verify_distribution.py dist/agents-team-0.1.0.zip
```

预期结果：单元测试通过，分发包构建成功，解包后测试和临时项目初始化均通过。

## 仍需注意

GitHub Issue 反查依赖 Actions 环境中的 `GITHUB_TOKEN` 和 `issues: read` 权限。缺少 token 或权限时必须失败，不得降级为“仅检查 PR 正文”。这不是麻烦，这是机制可信度的底线。

## 后续任务提示词

Goal：继续把 Agents-Team 的验收从“字段存在”推进到“证据真实”。

请先阅读本审计文档和 PR #1。下一轮重点实现：验证 PR 里的测试证据是否至少包含命令、退出码和结果；验证 QA 结论必须来自独立上下文；对 L3 任务增加成本、密钥、真实 provider、回滚和 smoke 证据字段。补测试、更新文档和分发验证，继续提交独立 PR，不直接合并默认分支。
